import _ from 'lodash';

const initState = {
    docs: {
        title: 'next static page test1'
    }
};
export default (state = initState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};
